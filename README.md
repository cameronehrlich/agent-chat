# Agent Chat CLI

`ac` is a stateless IRC coordination tool built on top of Ergo. Use it to send quick
messages, fetch history via CHATHISTORY, list channels, and manage your credentials.

See `SPEC.md` for the full design.
